## Phase 4 Complete: Plots, CLI Polish, and Reference Update

Added matplotlib plots (width ratio vs N_d, absolute widths vs N_d), a formatted textual summary, and `run_phase4_validation()` that reads from existing CSVs without re-running the 36-case sweep. Updated the intervalinf living reference with a detailed script entry. All phases now run end-to-end in ~110 s.

**Files created/changed:**
- `intervalinf/rough_work/bg_dli_1d_sweep.py`
- `intervalinf/docs/agent-docs/references/living/intervalinf-reference.md`

**Functions created/changed:**
- `_figures_dir()` — returns `Path(__file__).parent / FIGURES_FOLDER`, creates on demand
- `plot_width_ratio_vs_nd(summary_rows, figures_dir)` — errorbar plot of BG/DLI width ratio mean/std vs N_d, median dots, dashed y=1 reference; saves PNG + PDF
- `plot_absolute_widths_vs_nd(summary_rows, figures_dir)` — two-line plot of DLI and BG mean widths vs N_d; saves PNG + PDF
- `print_sweep_summary(summary_rows)` — formatted table with autogenerated monotonicity trend line
- `run_phase4_validation(verbose)` — reads summary CSV, calls all plot/summary functions, asserts PNG files exist, prints "Phase 4 PASSED"
- `main()` — updated to call `run_phase4_validation(verbose=True)` and updated title to "Phases 1-4"
- Matplotlib imports restructured: `import matplotlib` + `matplotlib.use("Agg")` + `import matplotlib.pyplot as plt` placed after all pygeoinf/intervalinf imports to avoid E402 linter warnings

**Tests created/changed:**
- `run_phase4_validation`: asserts `width_ratio_vs_nd.png` and `widths_vs_nd.png` exist in figures directory
- Full script runs to completion confirming "Phase 4 PASSED"

**Review Status:** APPROVED (after fixing living reference omission and E402 import ordering)

**Git Commit Message:**
```
feat(rough_work): add plots and summary for bg dli sweep

- Add plot_width_ratio_vs_nd: errorbar plot with median dots and y=1 reference
- Add plot_absolute_widths_vs_nd: DLI/BG mean width comparison
- Add print_sweep_summary: formatted table with trend line
- Add run_phase4_validation: reads CSVs, saves PNG/PDF, asserts files exist
- Update intervalinf living reference with bg_dli_1d_sweep.py entry
- Fix matplotlib import order to avoid E402 warnings

Plan: intervalinf/docs/agent-docs/completed-plans/bg-dli-1d-sweep-plan.md
Phase: 4 of 4
Related: intervalinf/docs/agent-docs/completed-plans/bg-dli-1d-sweep-phase-4-complete.md
```
